pipeline {
    agent any

    stages {
        stage('Downstream Work') {
            steps {
                echo "Downstream pipeline was triggered by upstream"

                sh '''
                #!/bin/bash
                
                sleep 5 
                pwd
                ls -lrt
                echo "This is downstream job running"
                '''
            }
        }
    }
}